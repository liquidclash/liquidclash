[package]
name = "clash-fetcher"
version = "0.0.1"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["rustls-tls", "blocking"], default-features = false }

[profile.release]
opt-level = "s"
strip = true
lto = true
